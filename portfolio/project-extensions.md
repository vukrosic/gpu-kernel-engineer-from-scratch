# Project Extensions

- Add a tiled matmul reference implementation.
- Add a `triton` optional module with guarded imports.
- Add timing comparisons against NumPy baselines.
- Add a cache-friendly attention variant.
- Add a profiling report with memory access notes.
- Add a final attention/KV-cache capstone with a benchmark dashboard.
- Add a GPU test matrix that covers shape, dtype, and tolerance combinations.
