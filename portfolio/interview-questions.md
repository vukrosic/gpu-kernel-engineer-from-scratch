# Interview Questions

- Why does memory layout matter for GPU kernels?
- What is the difference between a naive and a tiled matmul?
- Why is softmax numerically unstable without stabilization?
- What does attention compute?
- What should you benchmark when comparing kernels?
- What would Triton add on top of the reference code here?
- How do you decide whether a kernel belongs in CUDA, Triton, or a PyTorch wrapper?
- What makes a benchmark trustworthy rather than just a timing number?
- Where does a fused kernel win, and when does fusion stop helping?
- What is the simplest way to explain KV cache to someone who knows matmul?
